# Test-specific overrides for security properties
jwt.secret=test-secret-key-with-at-least-32-characters-for-tests
jwt.expiration-ms=86400000
jwt.issuer=bloodmatch-test
google.maps.api-key=test-api-key

# Mail autoconfig so EmailNotificationService can load in SpringBoot tests
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test@bloodmatch.local
spring.mail.password=test
